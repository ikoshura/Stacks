<mica:MicaWindow x:Class="Stacks.FanView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        
        d:DataContext="{d:DesignInstance Type=local:FanView}"
        xmlns:local="clr-namespace:Stacks"
        xmlns:mica="clr-namespace:MicaWPF.Controls;assembly=MicaWPF"

        Title="FanView"
        SizeToContent="WidthAndHeight"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True">

    <Border Background="{DynamicResource App.BackgroundBrush}" 
            CornerRadius="12" 
            BorderThickness="1" 
            BorderBrush="{DynamicResource App.BorderBrush}"
            Padding="8">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            
            <ItemsControl x:Name="FileItemsControl" Grid.Row="0">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border x:Name="ItemBorder" Background="Transparent" CornerRadius="6" Margin="4" Padding="8" Cursor="Hand" MouseMove="File_MouseMove" MouseLeftButtonUp="File_MouseLeftButtonUp">
                            <StackPanel HorizontalAlignment="Center">
                                <Border CornerRadius="6" Width="56" Height="56">
                                    <Border.Background>
                                        <ImageBrush ImageSource="{Binding Thumbnail}" Stretch="UniformToFill"/>
                                    </Border.Background>
                                </Border>
                                <TextBlock Text="{Binding FileName}" Foreground="{DynamicResource App.PrimaryTextBrush}" TextTrimming="CharacterEllipsis" TextAlignment="Center" MaxWidth="70" Margin="0,6,0,0" FontSize="11" TextWrapping="Wrap" MaxHeight="30"/>
                            </StackPanel>
                        </Border>
                        <DataTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource App.HoverBrush}"/>
                            </Trigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.Style>
                    <Style TargetType="ItemsControl">
                        <Setter Property="ItemsPanel">
                            <Setter.Value><ItemsPanelTemplate><UniformGrid Columns="5"/></ItemsPanelTemplate></Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Current.Orientation, Source={x:Static local:SettingsManager.Instance}}" Value="Vertical">
                                <Setter Property="ItemsPanel">
                                    <Setter.Value><ItemsPanelTemplate><StackPanel Orientation="Vertical" HorizontalAlignment="Stretch" /></ItemsPanelTemplate></Setter.Value>
                                </Setter>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ItemsControl.Style>
            </ItemsControl>
            
            <StackPanel Grid.Row="1" Margin="0,4,0,0">
                <Rectangle Height="1" Fill="{DynamicResource App.BorderBrush}" Margin="8,0,8,8" Opacity="0.5"/>
                <Button x:Name="OpenInExplorerButton" Content="Open in File Explorer" HorizontalAlignment="Stretch" Margin="8,0,8,4" Click="OpenInExplorerButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="{DynamicResource App.HoverBrush}"/>
                            <Setter Property="Foreground" Value="{DynamicResource App.PrimaryTextBrush}"/>
                            <Setter Property="Padding" Value="10,10"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="Root" Background="{TemplateBinding Background}" CornerRadius="4">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Root" Property="Background" Value="{DynamicResource App.HoverBrushInverted}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
                
                <!-- *** TOMBOL CLOSE DITAMBAHKAN DI SINI *** -->
                <Button x:Name="CloseButton" Content="Close" HorizontalAlignment="Stretch" Margin="8,4,8,4" Click="CloseButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="{DynamicResource App.HoverBrush}"/>
                            <Setter Property="Foreground" Value="{DynamicResource App.PrimaryTextBrush}"/>
                            <Setter Property="Padding" Value="10,10"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="Root" Background="{TemplateBinding Background}" CornerRadius="4">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Root" Property="Background" Value="{DynamicResource App.HoverBrushInverted}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

            </StackPanel>
        </Grid>
    </Border>

    <!-- Animasi fade-in opsional, tetapi membuat UI terasa lebih baik -->
    <mica:MicaWindow.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0.0" To="1.0" Duration="0:0:0.150" />
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </mica:MicaWindow.Triggers>
    
</mica:MicaWindow>