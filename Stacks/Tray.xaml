<!-- FILE: Stacks/Tray.xaml -->
<tb:TaskbarIcon x:Class="Stacks.Tray"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Stacks"
             xmlns:tb="http://www.hardcodet.net/taskbar"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance local:TrayViewModel}"
             IconSource="{Binding IconSource}"
             ToolTipText="{Binding ToolTipText}"
             LeftClickCommand="{Binding ToggleFanViewCommand}">

    <tb:TaskbarIcon.ContextMenu>
        <local:MainContextMenu DataContext="{Binding}" />
    </tb:TaskbarIcon.ContextMenu>

</tb:TaskbarIcon>